# RPi 部署首日 Bug 清单 — 2026-04-12

## 已修复 ✅
1. openwakeword API 变更（wakeword_models → wakeword_model_paths, 删 inference_framework）
2. Groq API key 401 → 重新生成
3. farewell "再见" 走 LLM 浪费 3s → 提前拦截本地处理（127ms）
4. pyttsx3 男声 "在的" → 改用 MiniMax TTS
5. InsecureRequestWarning / CUDA warning 刷屏 → 已压制
6. PortAudio 缺失 → apt install libportaudio2
7. 音频默认设备 → .asoundrc card 2

## 待修复 🔴

### P0 — 核心体验
- [x] **VAD 截断太早** — ✅ Silero VAD 集成完成（sherpa-onnx 内置，threshold 0.5, silence 0.5s）
- [ ] **颜色指令失败** — "灯改成Tiffany蓝" Groq 返回 set_color value=0（错误），LLM 知道 #0ABAB5 但 tool 执行失败
- [ ] **上下文丢失** — "你再试一下hex" 被 Groq 当成 turn_on，不理解这是改颜色的延续
- [ ] **LLM 幻觉执行** — "已设#0ABAB5" 但实际没调用 tool（嘴上说做了但没做）

### P1 — 延迟优化
- [x] **天气/股票 LLM 转述耗时** — ✅ info_query 模板化（跳过 LLM 转述，省 ~1500ms）
- [x] **ASR 首次加载 2s** — ✅ 启动时预热 SenseVoice 模型（commit 59d2780）
- [ ] **info_query 路由误判** — "告诉我你现在有" 被当成天气查询

### P2 — 待实现
- [ ] **小月不知道自己的情感映射** — 用户问"语气为什么变了"她不知道
- [ ] **TTS 回声偶发触发唤醒词** — sleep(1.0) 缓解但未根治，等 ReSpeaker AEC
- [x] ⏳ (待审查) **requirements-pi.txt 同步** — 加入 openwakeword

### 观察
- 本地指令（开关灯）体验很好：~500ms
- farewell 极快：~120ms
- 情绪检测→TTS 情感映射在真实场景下工作了（HAPPY→活泼语气）
- MiniMax TTS 缓存命中后秒出
- Groq 路由缓存命中 1ms
