# 工作记录 — 2026-04-02

## 项目总体进度

### Feature 完成状态

| # | 功能 | 状态 | 说明 |
|:---:|------|:---:|------|
| F0 | 新闻/股票 bug | ✅ closed | 多分类聚合 + CLI/语音双格式 + 中文化 |
| F1 | 意图路由 + 延迟优化 | ✅ closed | SenseVoice ASR(75ms) + VAD + 非阻塞TTS + LLM流式 |
| F2 | 人格系统 | ✅ closed | "小月"人设 + 情绪感知 + 动态prompt |
| F3 | 情境感知 | ⏸️ open | 需要 ESP32+PIR+DHT22 硬件 |
| F4 | 主动通知 | ⏸️ open | 依赖 F3 |
| F5 | Telegram 多渠道 | 🔲 open | 不依赖硬件，可以做 |
| F6 | 自然语言自动化 | ✅ closed | keyword/cron/once 触发器 + JSON持久化 |
| F7 | OLED Ambient | ⏸️ open | 需要 OLED 硬件 |
| F8 | 自诊断自修复 | 🔲 open | 不依赖硬件，可以做 |
| F9 | 开发者模式 | 🔲 open | 依赖 F8 |
| F10 | 行为学习 | ⏸️ open | 依赖 F3+F6 |
| F11 | 渐进式信任 | 🔲 open | 依赖 F10 |
| F12 | 自编程+监督 | 🔲 open | 依赖 F1-F9 |
| F13-F17 | 远期功能 | 🔲 open | 语音快捷词/知识积累/数字生活/多房间/情感识别 |

### 代码统计

- 测试: **490 passing**, 覆盖率 **82%**
- 14 个 commits (从 initial commit 到 personality rewrite)
- 核心模块: core/ (12 files), skills/ (10 files), devices/ (8 files)

---

## 2026-04-02 工作内容

### 1. Kimi K2.5 接入 + API key 智能路由
- `core/llm.py`: 新增 `base_url` 支持 OpenAI 兼容 API
- API key 根据 `base_url` 自动选择对应环境变量
  - `deepseek` URL → `DEEPSEEK_API_KEY`
  - `moonshot` URL → `MOONSHOT_API_KEY`
  - 其他 → `OPENAI_API_KEY`
- 实测：Kimi 余额不足 → 切换 DeepSeek → DeepSeek 拒绝情感表达 → 最终用 GPT-4o-mini

### 2. Azure Neural TTS 情感语音
- `core/tts.py`: 新增 `_speak_azure()`, SSML 风格控制, 20种情感
- SenseVoice 情绪 → Azure TTS style 映射（HAPPY→cheerful, SAD→gentle, ANGRY→calm）
- SSML XML 注入修复 (`xml.sax.saxutils.escape`)
- 实测：声音偏机械，情感切换不够自然

### 3. MiniMax TTS 接入
- `core/tts.py`: 新增 `_speak_minimax()`, REST API, 9种情感预设
- 实测：余额不足无法使用

### 4. OpenAI gpt-4o-mini-tts 接入
- `core/tts.py`: 新增 `_speak_openai_tts()`, 自然语言 instructions 控制语气
- 实测：英文优化的模型，中文发音不自然，逐句TTS有明显停顿

### 5. 情绪管线完善
- `TranscriptionResult` 新增 emotion/event 字段
- 情绪从 SenseVoice → jarvis.py → LLM system prompt → TTS
- 情绪映射修正：用户SAD → 小月 gentle（安慰），不是 sad（跟着悲伤）

### 6. 人格 prompt 重写
- 从"AI助手规则集"变成"真人管家人设描写"
- "小月" 替代旧名称，去掉所有 AI/助手/人工智能 字眼
- 三层人格模型：内核(性格) + 状态(时间/情绪) + 关系(记忆)
- prompt 从 800+ 字精简到 468 字

### 7. 测试覆盖率提升
- 62% → **82%** (目标 80% 达成)
- 257 → **490** tests (+233)
- 新增测试：cache, scheduler, realtime_data skill, TTS emotion, audio recorder VAD

### 8. Bug 修复
- stdin 缓冲区溢出：多按 Enter 导致连续录音
- SSML XML 注入：文本含 `<>&` 会破坏 Azure TTS
- API key 优先级：MOONSHOT_API_KEY 覆盖 DEEPSEEK_API_KEY
- 所有语音回复改为中文

### 9. 调研完成（结果存 notes/）
- 中文 LLM API 全面对比（DeepSeek/Qwen/Zhipu/Kimi/SiliconFlow）
- 情感 TTS 方案对比（Azure/MiniMax/OpenAI/CosyVoice）
- ASR 方案调研（SenseVoice/Groq/Google/Azure 等 10+ 方案）

---

## 待解决问题

### TTS 声音质量（当前最大痛点）
四个 TTS 引擎都试过了，没一个满意：
- **OpenAI TTS**: 中文发音像外国人，逐句有停顿
- **Azure**: 声音机械，播音腔
- **MiniMax**: 质量最好但余额不足
- **Edge TTS**: 免费但没情感

**下一步方案**：
1. 充值 MiniMax（最快解决）
2. 解决逐句TTS停顿问题（合并短句再调API）
3. 探索 CosyVoice 本地部署（Mac GPU 可跑）

### DeepSeek 拒绝情感表达
DeepSeek 的安全训练太强，personality prompt 压不住。
已切换到 GPT-4o-mini 解决。

### LLM 选择结论
| 场景 | 推荐 | 原因 |
|------|------|------|
| 闲聊/情感 | GPT-4o-mini | 遵从 prompt，不拒绝情感 |
| 性价比 | DeepSeek V4 | $0.30/M，但拒绝情感 |
| 中文最强 | DeepSeek V4 | 中文理解最好 |
| 意图路由 | Groq (llama-3.3-70b) | 免费+极快 |

---

## Session 2 — 记忆系统（同日后续）

### 项目全面审查
- 完整梳理了数据流（麦克风 → ASR → 路由 → LLM → TTS）
- 发现核心问题：小月是"云端大脑+本地四肢"，记忆存了但从未注入 prompt
- 确定方向：本地大脑 = 记忆系统 + 知识检索 + 减少云端依赖

### 记忆系统设计 + 研究
- 对比 xiaozhi-esp32-server、Mem0、MemGPT/Letta、EMem (ICLR 2025)
- 研究 bge-small-zh-v1.5 vs potion-base-8M vs multilingual-e5-small
- 确定技术栈：FastEmbed ONNX + numpy cosine + SQLite
- 设计记忆生命周期 6 环节：提取 → 去重 → 存储 → 检索 → 注入 → 维护

### 记忆系统实现（2791 行新代码）
- `memory/embedder.py` — bge-small-zh-v1.5 via FastEmbed，线程安全
- `memory/store.py` — SQLite 三表（memories/profiles/episodes），schema 迁移
- `memory/retriever.py` — 多信号检索 + category-dependent 衰减 + access 强化
- `memory/manager.py` — query/save/maintain 完整管线
- 集成到 personality.py / llm.py / jarvis.py / memory_skill.py
- 63 新测试，614 总测试全过

### 关键技术决策
- 6 种 category：identity/preference/relationship/event/task/knowledge
- 去重：key-first 确定性 + embedding 模糊（同 category 0.55 / 跨 category 0.7）+ LLM 终裁
- 检索：<100 条全量注入，≥100 条三层检索
- 衰减：identity 365天 / preference 180天 / event 30天 / task 14天半衰期
- 周维护：每周日 3am 合并重复

---

## 下一步计划

### 近期（不需要硬件）
1. ~~对话记忆注入 prompt（跨会话连续性）~~ ✅ 已完成
2. 世界知识库（Level 1 — 用户可以教小月知识）
3. F5: Telegram 多渠道（远程控制）
4. F0/F2/F6 代码审查完成

### 中期（需要硬件）
5. F3: 情境感知（ESP32+PIR+DHT22）
6. F4: 主动通知（依赖 F3）
7. F7: OLED Ambient（OLED 屏）

### 长期
8. F10-F12: 行为学习 + 渐进式信任 + 自编程
